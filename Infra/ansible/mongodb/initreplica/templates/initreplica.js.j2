rs.initiate({
  _id: "{{ replica_set_name }}",
  version: 1,
  members: [
    {% for host in groups['mongodb'] %}
    { _id: {{ loop.index }}, host : "{{ host }}:27017" },
    {% endfor %}
    ]
  }
)

printjson(rs.status())